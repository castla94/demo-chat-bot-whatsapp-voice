{
  "name": "lambda-extension-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "index.mjs",
  "scripts": {
    "check": "node --check index.mjs",
    "install:lambda:x64": "npm install --platform=linux --arch=x64",
    "install:lambda:arm64": "npm install --platform=linux --arch=arm64"
  },
  "engines": {
    "node": "22.x"
  },
  "dependencies": {
    "ffmpeg-static": "5.3.0"
  }
}
